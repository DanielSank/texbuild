\levelstay{Sea Turtle}
Sea turtles can swim really far.
Their max swim distance is computed as
\begin{equation}
  d(s) = \int_0^s \frac{\cos(x)}{\tan(x) + \sin(x)}a \, dx \label{swim_distance} % export
\end{equation}
where $s$ is the size of the turtle.
